<body>
  <h1> Reviews for <%= @movie.title %></h1>

    <%= form_tag '/reviews', method: :post do %>
      <%= render 'form' %>
    <%= submit_tag "Add your review", class: 'btn btn-primary' %>
  <% end %>
</body>
